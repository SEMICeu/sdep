[project]
name = "sdep-backend"
version = "0.1.0"
description = "SDEP backend"
authors = [
    {name = "ICTU"}
]
license = {text = "MIT"}
requires-python = ">=3.13,<3.14"  # Pin to 3.13 until psycopg2-binary has wheels for 3.14
keywords = ["fastapi", "pydantic", "sqlalchemy", "alembic"]

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.13",
    "Framework :: FastAPI",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
]

dependencies = [
    # Core FastAPI dependencies
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
    "python-multipart>=0.0.31",
    # Database dependencies
    "sqlalchemy>=2.0.36",
    "alembic>=1.14.0",
    "psycopg2-binary>=2.9.10",
    "asyncpg>=0.30.0",
    "greenlet>=3.1.1",
    # Data validation and settings
    "pydantic>=2.10.3",
    "pydantic-settings>=2.7.0",
    "email-validator>=2.2.0",
    "pydantic-extra-types[pycountry]>=2.0.0",
    # Authentication and security
    "pyjwt[crypto]>=2.13.0",
    # Configuration
    "python-dotenv>=1.0.1",
    "httpx>=0.28.1",
    # Caching
    "cachetools>=6.1.0",
    "clamd>=1.0.2,<2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pytest-mock>=3.14.0",
    "pytest-env>=1.1.5",
    "httpx>=0.28.1",
    "ruff>=0.10.4",
    "pyright>=1.1.409",
    "pre-commit>=4.0.1",
    "factory-boy>=3.3.1",
    "faker>=33.1.0",
]

[project.scripts]
sdep-server = "app.main:main"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pytest-mock>=3.14.0",
    "pytest-env>=1.1.5",
    "httpx>=0.28.1",
    "ruff>=0.10.4",
    "pyright>=1.1.409",
    "pre-commit>=4.0.1",
    "factory-boy>=3.3.1",
    "faker>=33.1.0",
    "pytest-xdist>=3.8.0",
    "aiosqlite>=0.20.0",
    # Used for API PDF generation
    "reportlab>=4.4.10",
]

[tool.uv]
# Transitive-dependency security floors. Per the pinning policy (docs/SECURITY.md
# "Dependency Version Pinning"): pyproject.toml declares the minimum acceptable
# version (the intent), while uv.lock records the exact, hash-verified version
# that ships (enforced at build time by `uv sync --frozen`). Security fixes are
# therefore expressed as a `>=` floor at the version that fixes the CVE - never
# `==`, which would duplicate the lock, block future fixes, and not cover
# transitive packages.
#   idna>=3.15           - CVE-2026-45409 (GHSA-65pc-fj4g-8rjx): DoS in idna.encode(), fixed in 3.15.
#   starlette>=1.3.1     - CVE-2026-48710 (BadHost): improper Host-header validation, fixed in 1.0.1.
#                          Also fixes CVE-2026-54282 and CVE-2026-54283 in request URL/form handling.
#   cryptography>=48.0.1 - CVE-2026-45447: heap use-after-free in OpenSSL PKCS7_verify. The
#                          wheel statically bundles OpenSSL (48.0.0 -> 4.0.0, vulnerable;
#                          48.0.1 -> 4.0.1, fixed), which Trivy cannot see inside the wheel,
#                          so this floor - not the CVE allowlist - is the control. SDEP never
#                          calls PKCS7_verify, so the flaw is unreachable; this is defense-in-depth.
# All three are transitive dependencies only; declared here, not as direct project dependencies.
constraint-dependencies = ["idna>=3.15", "starlette>=1.3.1", "cryptography>=48.0.1"]

[tool.uv.sources]
# Ensure we use the restructured backend source
app = { workspace = true }

[tool.ruff]
line-length = 88
target-version = "py313"
src = ["backend"]
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "build",
    "dist",
    "frontend",
    "alembic/versions",
]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "UP",   # pyupgrade
    "RUF",  # ruff-specific rules
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "TCH",  # flake8-type-checking
]

ignore = [
    "E203",  # whitespace before ':'
    "E501",  # line too long
    "E712",  # avoid equality comparisons to False - needed for SQLAlchemy boolean queries
    "RUF012", # mutable class attributes
    "TC003", # datetime is needed at runtime for models
    "B008",  # function call in argument defaults - standard FastAPI Depends() pattern
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # Allow unused imports in __init__.py
"backend/app/schemas/*.py" = ["TCH002"]  # Allow runtime imports used by Pydantic annotations
"app/schemas/*.py" = ["TCH002", "N815"]  # Allow runtime imports and camelCase for API contracts
"**/tests/**" = ["S101"]  # Allow assert statements in tests
"app/api/common/routers/*.py" = ["N803"]  # Path parameters must match URL (e.g., areaId)
"app/api/domains/ca/routers/*.py" = ["N803"]  # Path/form parameters must match the CA API contract

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ty.environment]
python-version = "3.13"

# Ignore rules that produce false positives for Pydantic/FastAPI patterns
[tool.ty.rules]
invalid-argument-type = "ignore"  # Pydantic model_construct and FastAPI middleware patterns
invalid-assignment = "ignore"     # FastAPI openapi override
unused-ignore-comment = "ignore"  # Alembic generated code has type: ignore comments

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "basic"
include = ["app"]
exclude = [
    "**/__pycache__",
    "**/node_modules",
    ".venv",
    "alembic/versions",
]
reportMissingTypeStubs = false
reportUnusedImport = true
reportUnusedVariable = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--tb=short",
    "-v",
    "-n", "auto",  # Enable parallel execution
]

asyncio_mode = "auto"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "unit: marks tests as unit tests",
    "auth: marks tests that require authentication",
    "database: marks tests that require database access",
]

filterwarnings = [
    # Fail tests on warnings unless a warning is explicitly allowed below
    "error",
    # Suppress Pydantic serialization warnings for intentionally invalid test data
    # These warnings occur when validation errors include the original invalid data
    # (e.g., string where int expected). The validation errors array shows what's wrong.
    "ignore:Pydantic serializer warnings:UserWarning:pydantic.main",
    # Suppress coroutine warnings
    "ignore:coroutine 'Connection._cancel' was never awaited:RuntimeWarning",
    "ignore:coroutine '.*' was never awaited:RuntimeWarning",
    "ignore:.*coroutine.*was never awaited:RuntimeWarning",
    "ignore::pytest.PytestUnraisableExceptionWarning",
    # Suppress SQLAlchemy DISTINCT ON PostgreSQL-specific warning
    # We intentionally use DISTINCT ON as this project uses PostgreSQL exclusively
    "ignore:DISTINCT ON is currently supported only by the PostgreSQL dialect:sqlalchemy.exc.SADeprecationWarning",
]

env = [
    "API_MODE=internal",
]

[tool.coverage.run]
source = ["app"]
omit = [
    "*/tests/*",
    "*/migrations/*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/.venv/*",
    "app/tests/*",
    "alembic/*",
    "*/__init__.py",
    "*/orm_integration.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.hatch.build.targets.wheel]
packages = ["backend/app"]
sources = ["backend"]

[tool.hatch.build.targets.sdist]
include = [
    "backend/app/**/*.py",
    "backend/pyproject.toml",
]

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"
